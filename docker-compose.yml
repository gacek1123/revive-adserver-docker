version: '3.8'

services:
  web-app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./installer.conf.php:/tmp/installer.conf:ro
    environment:
      - SERVICE_FQDN_PHP_FPM=${COOLIFY_URL}

  nginx:
    image: nginx:alpine
    restart: always
    expose:
      - "80"
    volumes:
      - revive-data:/var/www/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web-app

volumes:
  revive-data: